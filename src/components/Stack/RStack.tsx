import React from "react";
import { Button, Input, Stack } from "rsuite";

const RStack = () => {
  return (
    <div>
      <Stack>
        <Button>Click</Button>
        <Input />
      </Stack>
    </div>
  );
};

export default RStack;
